# Copy this file to .env.local (ignored by git) and adjust values for local development.

# Application environment (development | staging | production)
APP_ENV=development

# Admin master passwords (do NOT commit real values)
ADMIN_MASTER_PASSWORD_DEV=changeme-dev-secret
ADMIN_MASTER_PASSWORD=changeme-prod-secret

# Database connection overrides for local development (optional)
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=chessaroo
# DB_USER=chessaroo_user
# DB_PASSWORD=chessaroo_pass
